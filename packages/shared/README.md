# @basilisk/shared

Shared TypeScript types, config loading, and utilities used across services.

**Owns:** cross-cutting

This is a scaffold. Implementation tracked via the `US-*` GitHub issues for the epics above. See [`docs/BASILISK_MVP_PLAN.md`](../../docs/BASILISK_MVP_PLAN.md).

```bash
npm run dev --workspace @basilisk/shared        # watch mode
npm run typecheck --workspace @basilisk/shared
```
