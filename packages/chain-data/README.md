# @basilisk/chain-data

Cardano data-access client lib: Demeter (Ogmios/Kupo/db-sync) primary + Blockfrost fallback w/ failover.

**Owns:** EPIC-0 (US-0.1)

This is a scaffold. Implementation tracked via the `US-*` GitHub issues for the epics above. See [`docs/BASILISK_MVP_PLAN.md`](../../docs/BASILISK_MVP_PLAN.md).

```bash
npm run dev --workspace @basilisk/chain-data        # watch mode
npm run typecheck --workspace @basilisk/chain-data
```
