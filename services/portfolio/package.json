{
  "name": "@basilisk/portfolio",
  "version": "0.1.0",
  "private": true,
  "description": "Resolves UTxOs to holdings, marks to market, computes P&L; powers portfolio + wallet profiler.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
