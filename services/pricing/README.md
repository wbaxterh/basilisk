# @basilisk/pricing

DEX adapters + VWAP pricing + OHLCV candle engine + quality filter + token metadata.

**Owns:** EPIC-0 (US-0.3, US-0.4, US-0.5, US-0.6, US-0.7)

This is a scaffold. Implementation tracked via the `US-*` GitHub issues for the epics above. See [`docs/BASILISK_MVP_PLAN.md`](../../docs/BASILISK_MVP_PLAN.md).

```bash
npm run dev --workspace @basilisk/pricing        # watch mode
npm run typecheck --workspace @basilisk/pricing
```
